<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
    <RootNamespace>EvenTech.Services</RootNamespace>
    <AssemblyName>EvenTech.Services</AssemblyName>
    <Nullable>disable</Nullable>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>
  <ItemGroup>
    <!-- DPAPI (ProtectedData) viene con el runtime de escritorio de .NET 8, el mismo que
         necesita la interfaz: se toma de ahi y no de un paquete NuGet. -->
    <FrameworkReference Include="Microsoft.WindowsDesktop.App" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\EvenTech.BE\EvenTech.BE.csproj" />
  </ItemGroup>
</Project>
